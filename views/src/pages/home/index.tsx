import { Button } from 'antd';
import Modal from '@/components/Modal/modal';

export default function Index() {
  // Modal();
  return (
    <div>
      <Button onClick={() => { Modal(); }}>12323123</Button>
    </div>
  );
}
